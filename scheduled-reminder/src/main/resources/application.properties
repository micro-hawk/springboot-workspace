server.port=7823
server.context.path=/test

# Database Configuration
spring.data.mongodb.database=ehr-main
spring.application.name=flex-ehr-main

# spring.data.mongdb.username=localhost

spring.security.user.name=user
spring.security.user.password=user

# Actuator Config
management.endpoints.web.base-path=/manage
management.prometheus.metrics.export.enabled=true
management.endpoints.web.exposure.include=health,metrics,prometheus,info

# Logging Configuration
#logging.level.org.springframework.data.mongodb.core.ReactiveMongoTemplate=ERROR
# logging.config=http://${spring.cloud.consul.host}:${spring.cloud.consul.port}/v1/kv/config/${spring.application.name}/logback.xml?raw=true&xml
security.enable-csrf = false

service.garuda.endpoint=http://${spring.cloud.consul.host}:7890/garuda
#service.garuda.endpoint=http://127.0.0.1:7890/garuda
